{
  "name": "bagagedrager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp public/sw.js dist/sw.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.3"
  }
}
