{
  "name": "bagagedrager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp public/sw.js dist/sw.js && cp public/manifest.json dist/manifest.json",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "check": "svelte-check --threshold error"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.56.4",
    "svelte-check": "^4.7.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3"
  }
}